name: blank

on:
  push:

jobs:
  undergo:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        setting: ["pregnant", "post", "uncertain", "host", "ally"]
        proper: ["justice", "deliberate", "project", "cup"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar nT
